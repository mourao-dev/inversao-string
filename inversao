# include <stdio.h>
# include <string.h>


int main (void){

	char palavra[100];
	scanf("%s",palavra);

	int t = strlen(palavra);
	
	for (int i = t;i >= 0; i-- ){
		
		printf("%c", palavra[i]);
		
		}
	
	return 0;
	}
